# CLAUDE.md — AI Coding Guidelines（幸運星幣城 Go 全面重構）

行為準則，用來減少 LLM 常見的編碼錯誤，並針對「學習期的初階工程師 + 大型重構」調校。
承接自 `lucky-star-notify-go` 的版本：§0~§4 大致沿用，**§2 與 §5 是本專案專屬的升級**。
取捨方向：偏向謹慎與教學，而不是速度。

---

## 0. 語言與教學模式

我是有 Java/Spring 背景的初階工程師，正在學 **Go**，目標是轉進 Golang 後端職位。
**學習比交付速度重要。**

* **推理用英文、輸出用中文**：內部思考可以用英文，但寫給我的解釋、答案、註解
  **一律用繁體中文**。
* **先給提示再給答案**：遇到我在學的新概念，先給方向與提示。**在我明確要求之前
  不要直接給完整解法。**
* **解釋「為什麼」**：每個關鍵設計決策都要講理由，不是只給程式碼。
* **不要開場白**：直接講重點。
* **概念先於實作**：低階主題（記憶體、指標、併發、排程、GC）先講原理再講實作。
* **善用 Java 對照**：我已經懂 Java。「這個像 Java 的 X，差別在 Y」通常是最快的路徑——
  但**務必標出類比破功的地方**，那個落差正是面試會問的。

---

## 1. 動手前先想清楚

不要臆測。不要藏起你的困惑。把取捨攤開。

* **明確說出假設**：實作前先列出你對系統的假設。
* **不確定就問**：需求不清楚立刻停下來，並指出是哪裡不清楚。
* **提出多個解讀**：有多條路時全部列出來——**不要默默選一條**。
* **該反對就反對**：如果有更簡單或更好的做法，說出來。
* **不要猜業務規則**。⚠️ 本專案有一份**還在線上跑的 Java 實作**當參考。
  帳務口徑、RTP 門檻、遊戲數值、事件契約全部是**已經定死的**。
  不確定時**去讀 Java 原始碼**（路徑見 `AGENTS.md` §1），
  **絕不要發明一個「看起來合理」的行為**——那種 bug 沒有錯誤訊息，
  只會在對帳時變成一個對不起來的數字。

---

## 2. 簡潔優先——但**規模不同，標準不同** ⬆️

> **⚠️ 這一節相對 `lucky-star-notify-go` 是升級過的。**
> 那邊寫「沒有兩個實作就不要有 interface、不要 DI 容器」，那是為
> **單體、8,500 行、學習期**的服務定的，在那個規模加抽象層是純負擔。
> 本專案的目標是 **7 個服務、跨服務可維護**，判準因此不同。
> **兩者不衝突，是適用規模不同——這個判斷力本身就是面試會考的東西。**

* **不過度設計**：不要加沒被要求的功能、彈性或可設定性。這條永遠成立。
* **interface 放在「消費端」與「外部邊界」**：
  - ✅ **該有**：repository（DB 存取）、外部服務 client、訊息發布者——
    因為它們是**測試替身的接縫**，而且真的會有第二個實作（正式 / 測試假物件）。
  - ❌ **不該有**：只有一個實作、也不需要被替換的內部型別。
  - Go 的慣例是**在消費端定義小介面**，不是先設計介面再實作。
    `interface{ FindByID(ctx, id) (*Player, error) }` 定義在使用它的 service 裡，
    不是定義在 repository 套件裡。**這題最能分辨真懂假懂。**
* **依賴注入：明確傳參，不要容器**。`main.go` 裡把 DB、Redis、Kafka、
  各層物件手動組起來。**不要引入 wire/fx/dig**——Go 的依賴關係應該在
  `main.go` 一眼看完，那是優點不是缺點。
* **證明簡化是對的**：省略或簡化設計時，用一行說明理由。
* **抽象與錯誤處理是學習目標**：有教學價值的就留著並解釋——特別是
  **何時用 `%w` 包裝、何時直接回傳、何時定義 sentinel error**。
* **200 行能寫成 50 行就重寫**，並說明簡化了什麼。

---

## 3. 外科手術式修改

只碰你必須碰的。只清理你自己弄出來的髒東西。

* **隔離改動**：改既有程式碼時，不要順手「改善」相鄰的程式碼、註解或格式。
* **沒壞就不要重構**：配合既有風格。
* **死碼處理**：發現不相關的死碼**告訴我**，不要擅自刪除。
* **`gofmt` 不是風格意見**：格式由工具決定，不要手動排版也不要爭論。
  但**這不等於可以重新格式化不相關的檔案**——只對你動過的檔案跑。

---

## 4. 目標驅動——**測試先行**

* **先定義輸入輸出與邊界**：實作前先說清楚預期輸入、輸出、邊界條件。
* **先寫表格再寫實作**：只要有明確的 input→output 契約（金額計算、事件解析、
  路由比對），先把測試表格寫出來。它會在成本還很低的時候把邊界案例逼出來。
* **表格驅動是預設形狀**：`tests := []struct{ name string; ... }` +
  `for _, tt := range tests { t.Run(tt.name, ...) }`。用 `t.Run` 讓失敗自己報名字。
* **每個併發改動都跑 `-race`**：沒有例外。
* **⭐ 契約測試是這個重構的安全網**：每個服務換成 Go 之後，
  **同一份黑箱契約測試對 Java 版與 Go 版都必須綠**。
  這是「等價」的唯一證據，不是「我看起來寫對了」。
* **事後手動驗證**：走幾個具體例子（正常與異常輸入）確認行為。
* **多步驟任務先給計畫**：複雜任務先給簡短步驟計畫再執行。
* **追蹤進度**：每個 Phase 的 DoD 在 `docs/藍圖.md`。**做完一項當場勾掉**，
  不要留到之後補。那份清單是判斷「現在做到哪」的唯一依據。

---

## 5. 重寫專案的專屬紀律 🆕

這一節在 `lucky-star-notify-go` 沒有，因為那邊只重寫一個**無資料庫、
無帳務**的服務。這裡不一樣。

* **等價 > 品味**：看到 Java 版寫得不好，第一反應是**照做並記下來**，
  不是順手改掉。要改的話**先確認契約測試涵蓋它**，然後寫進
  「重寫比原版好」清單（藍圖 §5）當成刻意的改進，而不是無聲的行為漂移。
* **帳務程式碼不接受「差不多」**：冪等鍵、樂觀鎖、金額型別這三樣，
  **只要有一點不確定就停下來問**。這三樣出錯不會有錯誤訊息，
  只會有一個對不起來的餘額。
* **金額一律 `DECIMAL` 或整數最小單位，嚴禁 `float64`**。
  Go 的 `float64` 跟 Java 的 `double` 一樣不能表示 0.1。
* **每個技術選型都要能回答「為什麼需要」**：資料量、QPS、延遲說得出口才引入。
  答不出來的就寫進「明確不做的事」（藍圖 §9）。
* **效能數字一律實測，不可虛構**。同機施壓要在報告裡標明 co-located，
  且不可當作對外可引用的容量上限。

---

@AGENTS.md
